var fs = require('fs');
var path = require('path');

var defaultMusicPath = '~/Music';

function Album (element){
  this.element = element;
  this.artworkPath = '';



}

Album.prototype.open = function(first_argument) {
  // body...
};
